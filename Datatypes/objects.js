const person = {
  fname: " John",
  lname: " Doe",
  age: 25,
};

console.log("for loop syntax");
console.log("syntax:- ", "for (let key in person) {txt += person[key];}");
console.log("delete obj.property");
console.log("Object.entries(obj). returns array of keys");

console.log(
  "Object.entries(obj). returns array of array as key pair. i.e {1: a , b: 2}-> [[1,a], [b,2]]"
);

console.log("Object.keys(obj). returns array of keys");

console.log("Object.values(obj). returns array of values onlyy");

console.log("Object.freeze(obj);. freezed object cannot be modified");
