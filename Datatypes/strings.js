let str = "It's alright";

console.log("str.length", str.length);

console.log("const x = str[i] is valid but str[i] = x is not valid");

console.log("different methods are as follows");

console.log(
  "There are 3 methods for extracting a part of a string: slice(start, end),substring(start, end),substr(start, length)"
);

console.log("different methods are as follows");

console.log("different methods are as follows");

console.log("different methods are as follows");

console.log("different methods are as follows");

console.log("different methods are as follows");

console.log(
  "escape character",
  "' single quote , \" double quote, \\ backslash",
  "\n new line"
);
